#list information about block devices (like hard drives, SSDs, NVMe, partitions, USB drives).

lsblk 

#report file system disk space usage.

df 

#report file system disk space usage in human readable format.

df -h